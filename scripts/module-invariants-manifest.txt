# Known-unreachable live modules (C6 rot guard).
#
# A module listed here is NOT reachable from any Lake target root
# (`lean_lib FormalSystem`, `lean_lib BimodalTest`, or any `lean_exe` root), so
# `lake build` never compiles it and a broken import inside it would go
# unnoticed. C6 compile-checks each entry in isolation with `lake env lean`.
#
# Rules:
#   - C6 FAILS if an unreachable live module is missing from this file.
#   - C6 FAILS if an entry names a module that no longer exists.
#   - Wiring a module into the build graph means DELETING its line here.
# One module path per line; `#` starts a comment.

# --- Root ------------------------------------------------------------------
# The intended root file for FormalSystem (see FormalSystem/Init.lean itself and
# scripts/CheckInitImports.lean). No live module imports it yet -- rewriting the
# ~430-file import graph to route through it is an explicit, deferred follow-up
# (see the task that introduced this file), not this line's concern. DELETE this
# line when that rewrite lands and something in the build graph imports it.
FormalSystem.Init

# --- Automation ------------------------------------------------------------
# Benchmark harness. Its only importer is BimodalTest.Automation.ProofFirstTests,
# which cannot be wired into the test root (see the Tests section below), so this
# module stays outside the build graph too.
FormalSystem.Automation.ProofFirstBenchmark

# --- Metalogic -------------------------------------------------------------
# Sibling aggregators for directories whose contents are reached directly rather
# than through the aggregator. They exist so every Metalogic/ subdirectory has
# one, and these three have no importer: an aggregator imported by a file that
# its own contents already reach is how a module-level cycle gets introduced.
# Being unreachable, `lake build` never compiles them, so C6 does it here --
# which is what keeps a stale import inside one of them from going unnoticed.
#
# `FormalSystem.Metalogic.Algebraic` used to stand here as a fourth. It no longer
# does: `Metalogic.lean` now imports it, so the aggregator and its four children
# are reachable and `lake build` compiles them. Those five lines were deleted in
# that same commit, because C6 fails if an entry names a reachable module.
FormalSystem.Metalogic.Core
FormalSystem.Metalogic.Bundle
FormalSystem.Metalogic.SoundnessLemmas

# Children of the sibling aggregators just above. Each is reached ONLY through an
# aggregator that is itself listed above as deliberately importer-less, so it inherits
# that unreachability rather than having any of its own: nothing in the build graph
# imports it, and nothing in the build graph imports the only thing that does.
#
# DELETE a line here when the module it names acquires a direct importer outside its own
# aggregator -- that is exactly what wiring one of these in means.
#
# Coverage note, so the record is not read as claiming more than it delivers: these two
# are ALREADY compile-checked today, indirectly. C6 runs `lake build` on the manifested
# aggregators above, and that compiles their transitive dependencies, which includes every
# module in this block. Listing them here is rot-guard bookkeeping -- C6 fails on absence
# from this file independently of compile coverage -- not a new compile guarantee.
FormalSystem.Metalogic.SoundnessLemmas.CoValidity

# A genuine orphan: no importer anywhere in the live tree, aggregator or otherwise. Its
# sibling aggregator `Kamp/NfMultiAnchorBridge.lean` IS reachable (imported by
# `Kamp/KampPrior.lean`) and does import `NfMultiAnchorBridge.OuterGate` -- but not this
# module. The only other mention of it in the tree is prose, in
# `WeakCanonical/PriorExpressivenessDense.lean`.
#
# Unlike the block above, this line is a real coverage gain rather than bookkeeping:
# nothing else causes this module to be built, so until it was listed here it was compiled
# by nothing at all.
#
# The eventual fix is to wire it in with one `import` line in
# `Kamp/NfMultiAnchorBridge.lean`, beside the `OuterGate` import already there -- every
# other module in that directory is already reachable that way. That is a build-graph
# change, deliberately deferred; when it lands, DELETE this line in the same commit.
FormalSystem.Metalogic.WeakCanonical.Kamp.NfMultiAnchorBridge.OuterGateFaithful

# The bi-lasso decision layer is now registered: `Decidability.lean` imports the re-export
# `FormalSystem.Metalogic.Decidability.BiLasso`, so the layer is reachable from the Lake
# target roots and `lake build` compiles it. The fifteen module lines that used to stand
# here -- the aggregator and its fourteen imports -- were deleted in that same commit,
# because C6 fails if a manifest entry names a module that has become reachable.
#
# What remains below is the effective-periodic-extension cluster, which the re-export
# deliberately does not carry: `Agreement` has no importer anywhere, `Orbit` is imported
# only by `Agreement` and by the unreachable `BimodalTest.Metalogic.PeriodicExtensionAxiomTest`,
# and `Extend`/`Successor` only by `Orbit`. These four stay unreachable, and stay listed here
# so each is compile-checked in isolation, until that work wires itself in.
FormalSystem.Metalogic.Decidability.BiLasso.Extend
FormalSystem.Metalogic.Decidability.BiLasso.Successor
FormalSystem.Metalogic.Decidability.BiLasso.Orbit
FormalSystem.Metalogic.Decidability.BiLasso.Agreement

# The finite-carrier periodic extension theorem, additive over Semantics/Extension/, WAS listed
# here with the note "DELETE this line when it is registered". It is now registered in
# FormalSystem/Semantics.lean -- alongside LexCarrier and BLSchemaValidity, the other two
# modules that aggregator was missing -- so the line is gone and `lake build` guards it.

# --- Tests -----------------------------------------------------------------
# Test modules present under Tests/ but not imported by Tests/BimodalTest.lean,
# so `lake build BimodalTest` never compiles them.
# These two do not compile: they pass `String` where `Atom` is now expected,
# i.e. they predate the Atom type change and were never updated because nothing
# builds them. Recorded rather than re-discovered on every run.
broken: BimodalTest.ProofSystem.DerivationBenchmark
broken: BimodalTest.Semantics.SemanticBenchmark

# These two compile in isolation but cannot be imported by Tests/BimodalTest.lean:
# each pulls in an executable root that defines `main`, colliding with the `main`
# from FormalSystem.Automation.DatasetValidator already in that environment. They stay
# unwired and compile-checked here.
BimodalTest.Automation.FormulaMutatorTest
BimodalTest.Automation.ProofFirstTests

# Axiom-profile evidence for the effective periodic extension result. Not imported by
# Tests/BimodalTest.lean, and NOT unblocked by the bi-lasso re-export having landed: this
# module imports `...BiLasso.Orbit`, which is in the effective-periodic-extension cluster
# above and is deliberately outside the re-export, so it stays unreachable and this line
# stays. Compile-checked here in isolation instead. DELETE this line when `Orbit` becomes
# reachable and this module is imported by Tests/BimodalTest.lean.
BimodalTest.Metalogic.PeriodicExtensionAxiomTest

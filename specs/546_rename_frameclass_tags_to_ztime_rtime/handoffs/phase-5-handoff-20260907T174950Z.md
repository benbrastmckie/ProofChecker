# Phase 5 Handoff — Tier 1 constructors (atomic batch)

## Immediate next action
Open Phase 6 (string literals, round-trip parsers, Tests row names, generated artifacts).
Site list at `<scratchpad>/phase6-notes.md`.

## State
Phase 5 [COMPLETED]. `FrameClass.Discrete` -> `FrameClass.ZTime`, `FrameClass.Dedekind` ->
`FrameClass.RTime` across 81 live `.lean` files (493 `.Discrete` + 369 `.Dedekind` occurrences),
the `inductive FrameClass` constructor lines, and `scripts/typst-status-counts.sh`'s two greps.

Verification (Tier: full):
- `lake build` green (2591 jobs), `lake build BimodalTest` green (2642 jobs)
- `scripts/typst-status-counts.sh --json` still yields discrete_only_count=3,
  dedekind_only_count=3 (a 0 would have meant the greps did not move)
- full `scripts/check-module-invariants.sh` ALL CHECKS PASSED (C1/C2/C3/C14/C15/C16 all green)
- KEEP guard clean (560 / 43 / 45 / 53); zero residual `\.(Discrete|Dedekind)\b`

## Carried into Phase 6
Rename `DISCRETE_ONLY_COUNT`/`discrete_only_count` -> `ZTIME_ONLY_COUNT`/`ztime_only_count` and
the Dedekind pair, together with their `#discrete-only-count`/`#dedekind-only-count` consumers in
`typst/chapters/03-proof-theory.typ` and `typst/chapters/p2-frame-classes.typ`, then regenerate
`typst/generated/status.typ`. Both halves must move in one commit or `typst compile` breaks.

Also carried in: `resultDiscrete` / `discreteOk` local bindings in
`FormalSystem/Metalogic/Decidability/Saturation.lean:2428-2436` (class-naming locals plus a
`s!"Discrete={discreteOk}"` debug string).

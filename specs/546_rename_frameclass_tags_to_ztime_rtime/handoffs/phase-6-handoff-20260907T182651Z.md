# Phase 6 Handoff — string literals, parsers, generated artifacts

## Immediate next action
Open Phase 7 (Lean docstring rewrite). Locate each site by its quoted prose, not by line number.
Sites confirmed present: `Semantics/FrameClassValidity.lean` (interpretation table, "Naming
deviation of record" passage, the two `**not**` bullets), `Semantics/FrameProperty.lean:54` and
`:196-206` ("standard and unambiguous name"), `Semantics/Validity.lean:681`,
`Semantics/BLValidity.lean`, `Semantics/Correspondence/Indicator.lean`,
`ProofSystem/Axioms.lean:495-527`, and one extra the plan did not list:
`FormalSystem/Semantics.lean:70`.

## State
Phase 6 [COMPLETED]. Verification (Tier: full):
- `lake build` green (2591), `lake build BimodalTest` green (2642), `lake build dataset_generator`
  green (2840)
- `typst-sync-check.sh` Check 2 (status.typ byte-for-byte) PASS, Check 3 (machine appendix) PASS
- Round-trip smoke test: `lake exe dataset_generator --frame-class {ztime,discrete,rtime,dedekind}`
  all exit 0; emitted `frame_class` is `ZTime` for both ztime and discrete, `RTime` for both
  rtime and dedekind; the ztime and discrete metadata files are byte-identical modulo timestamps
- KEEP guard clean

## Known remaining typst-sync-check Check 1 violations (Phase 8 work)
11 caused by this task (`FrameClass.Dedekind`, `IsDedekind`, `TaskFrame.IsDedekind`,
`TaskFrame.IsSuccArchDiscrete`, `ValidDiscrete`, `completeness_discrete`, `completeness_dedekind`,
`completeness_dedekind_engine`, `consequence_completeness_dedekind`, `soundness_discrete`,
`soundness_dedekind`) plus 4 pre-existing at HEAD (`@[aesop norm unfold]`,
`@[aesop safe forward]`, `DenseValidity.lean`, `ValidDedekindDense`).

# Phase 1 Handoff — Tier 3a Metalogic class-level statements + C14 baselines

## Immediate next action
Open Phase 2 (Tier 3b: BL, Star, Conservativity, tableau class families). The sed script is
prepared at `<scratchpad>/phase2.sed`.

## State
- Phase 1 [COMPLETED]. `lake build` green; `scripts/check-module-invariants.sh` full run exits 0
  (ALL CHECKS PASSED, ~40 min runtime); KEEP-list guard clean (560 / 43 / 45 / 53).
- 37 live `.lean` files renamed; `scripts/check-module-invariants.sh` C14 baseline+probe pair and
  prose moved.

## Key decisions
- Full `check-module-invariants.sh` takes ~40 min (C1 builds FormalSystem AND BimodalTest, C16
  runs `lake exe runLinter`). Use `--no-build` for intermediate phases; full runs at Phase 5 and
  Phase 9 only.
- `typst-sync-check.sh` Check 1 already failed at HEAD with 4 pre-existing violations
  (`@[aesop norm unfold]`, `@[aesop safe forward]`, `DenseValidity.lean`, `ValidDedekindDense`).
  Phase 8/9 must fix forward or whitelist these; they are not caused by this task.
- Phase 5 qualifier survey confirmed: the only qualified forms of `.Discrete`/`.Dedekind` are
  `FrameClass.`, `ProofSystem.FrameClass.`, `FormalSystem.ProofSystem.FrameClass.` — the
  word-boundary sed is safe.

## Deviations
- C14 lines 871/873 + 929/931 (tmComplete* names) deferred to Phase 2.
- Added `not_derivable_nil_bot_discrete` -> `not_derivable_nil_bot_ztime`.

# Phase 3 Handoff — Tier 2 validity predicates + nolints.json

## Immediate next action
Open Phase 4 (frame predicates `IsSuccArchDiscrete`/`IsDedekind` + satellites). Sed prepared at
`<scratchpad>/phase4.sed`.

## State
Phase 3 [COMPLETED]. 36 `.lean` files renamed; `scripts/nolints.json` hand-edited (two docBlame
entries, never regenerated; still valid JSON).
Verification: `lake build` green (2591 jobs), `lake build BimodalTest` green (2642 jobs),
`lake exe runLinter FormalSystem` -> "Linting passed" (C16 clean, so the hand-edited nolints
entries still mask), KEEP guard clean, `--no-build` invariants ALL CHECKS PASSED.

## Counts
`Valid(ZTime|RTime)` now at 190 lines / 32 files — exactly the pre-rename
`Valid(Discrete|Dedekind)` figure. `ValidComplete` unchanged at 38.

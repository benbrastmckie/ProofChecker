# Phase 4 Handoff — frame predicates IsZTime / IsRTime

## Immediate next action
Open Phase 5, the atomic-batch constructor pass. Sed prepared at `<scratchpad>/phase5.sed`
(`s/\.Discrete\b/.ZTime/g`, `s/\.Dedekind\b/.RTime/g`). The `inductive FrameClass` constructor
lines in `ProofSystem/Axioms.lean` (currently ~532-533) have no leading dot and must be edited
by hand; so must `scripts/typst-status-counts.sh`'s two greps (`'=> \.Discrete'`, `'=> \.Dedekind'`).

## State
Phase 4 [COMPLETED]. 18 `.lean` files plus `scripts/boneyard-import-waivers.txt`.
`TaskFrame.IsSuccArchDiscrete` -> `TaskFrame.IsZTime`, `TaskFrame.IsDedekind` -> `TaskFrame.IsRTime`,
satellites `isZTime_of_instances` (11), `IsZTime.elim` (2), `isDense_of_isRTime` (4),
`isComplete_of_isRTime` (3).
Verification: `lake build` green (2591 jobs); zero residual `Is(SuccArchDiscrete|Dedekind)`;
`TaskFrame.IsDiscrete` unchanged at 8; KEEP guard clean; `--no-build` invariants ALL CHECKS PASSED.

## Phase 5 pre-checks already done
Qualifier survey confirms the only qualified forms are `FrameClass.`,
`ProofSystem.FrameClass.`, `FormalSystem.ProofSystem.FrameClass.` — the word-boundary sed is safe.
String literals `"Discrete"`/`"Dedekind"` carry no leading dot and are untouched by the sed
(Phase 6 handles them).

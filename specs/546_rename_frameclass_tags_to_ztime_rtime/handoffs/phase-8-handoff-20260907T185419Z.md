# Phase 8 Handoff — typst prose, docs sweep, naming-convention record

## Immediate next action
Open Phase 9, the final verification gate: full `lake build`, `lake build BimodalTest`, full
`scripts/check-module-invariants.sh`, `scripts/typst-sync-check.sh`, final KEEP guard and
whole-tree residual assertion, and `git diff --stat` Boneyard check.

## State
Phase 8 [COMPLETED]. 20 typst/docs files plus `README.md`, `specs/ROADMAP.md`,
`typst/sync-check-whitelist.txt`.
Verification: `typst-sync-check.sh` PASS on all three checks (was FAILing at HEAD before this
task started, with 4 pre-existing violations — 3 of those are now fixed forward and 1,
`ValidDedekindDense`, was a stale name repointed to `ValidRTime`);
`check-module-invariants.sh --no-build` ALL CHECKS PASSED; KEEP guard clean.

## The rule applied to prose
Renamed where the token names the **`FrameClass` tag** (backticked spans, "frame class X",
"the X class", enumerations of the four tags, diagram node labels, `#leansrc` identifier
arguments). Kept where it names the paper's **order property** ("a frame is *Discrete*",
`op("Log")("Discrete")`, "Dedekind-complete", "Dedekind cut"), the **Reynolds axiom layer**
(`layerReynoldsDedekind`), or a **module/file name** (deferred by decision 1).
`typst/FormalFoundations.typ` is the paper and keeps the property vocabulary throughout.
The full table is in the plan's Phase 8 "KEEP-boundary decisions" subsection.

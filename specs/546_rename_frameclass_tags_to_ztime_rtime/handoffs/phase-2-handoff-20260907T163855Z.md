# Phase 2 Handoff — Tier 3b BL / Star / Conservativity / tableau class families

## Immediate next action
Open Phase 3 (Tier 2 validity predicates + `scripts/nolints.json`). Sed prepared at
`<scratchpad>/phase3.sed`.

## State
Phase 2 [COMPLETED]. 25 `.lean` files renamed plus the four deferred C14 lines.
Verification: real `lake build` green (2591 jobs), fast C14 probe PASS, KEEP guard clean,
`check-module-invariants.sh --no-build` ALL CHECKS PASSED.

## CRITICAL correction carried forward
The build-guard invocation in the agent contract (`lake-build-guard.sh build --timeout 1800 --`)
has NO lake args after `--` and exits 77 (usage error) without building. The correct form is:
  bash .claude/scripts/lake-build-guard.sh build --timeout 1800 -- build
  bash .claude/scripts/lake-build-guard.sh build --timeout 1800 -- build BimodalTest
Phase 1's build claim rested on `check-module-invariants.sh`'s own real `lake build` +
`lake build BimodalTest` (ALL CHECKS PASSED at 7e304b6e9), which is stronger, so Phase 1 stands.

## Fast verification recipes (avoid the ~40 min full invariants run)
- `<scratchpad>/c14probe.sh` — reruns C14's `#print axioms` half alone (~1 min).
- `<scratchpad>/keepguard.sh` — KEEP-list guard (560 / 43 / 45 / 53).
- `bash scripts/check-module-invariants.sh --no-build` — structural pass (~1 min).
- Full run reserved for Phase 5 and Phase 9.

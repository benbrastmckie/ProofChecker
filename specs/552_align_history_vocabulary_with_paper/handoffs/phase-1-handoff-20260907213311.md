# Phase 1 handoff — task 552

- Phase 1 (record-file prose) COMPLETED. `specs/paper-definitions-of-record.md` edited at 6 prose
  sites (headings for `def:world-history`, `thm:extension`, `cor:occurrence`; the layering
  sentence; the `H_F` sentence; the box-clause commentary) plus one new addendum subsection.
- No `sha256:`, `FILE_CHECKSUM`, `PINNED_COMMIT`, or fenced-latex line changed.
- `check-paper-definitions.sh` drift set unchanged: {def:S5, def:BX, def:BX-z, def:BX-d,
  def:BX-r, def:TMplus}, 0 unresolved, exit 1 (baseline).
- Re-measured baselines (supersede the plan's figures where they differ):
  live-tree sorry = 331 (plan said 348); C19 refined = 92.33%; typst TOTAL_VIOLATIONS = 4;
  readme-lint PASS, broken refs 0, stale dates 11 (plan said <=2);
  `WorldHistory` = 620 occurrences over 71 live .lean files; `ConvexHistory` free in live tree.
- NEXT ACTION: Phase 2 — `git mv FormalSystem/Semantics/WorldHistory.lean ConvexHistory.lean`,
  sed over the explicit `git ls-files` list excluding Boneyard, then one guarded detached build.

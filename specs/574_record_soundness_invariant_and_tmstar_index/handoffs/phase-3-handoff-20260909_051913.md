# Phase 3 Handoff — Relocate the `StarValidIn` binder-shape adapters

**Status**: [COMPLETED]

## Immediate next action

Open Phase 4: measure `#print axioms` for the six TM⋆ headline declarations through the exact
C14 pipeline, then append the surviving lines to both heredocs in
`scripts/check-module-invariants.sh` (`C14BASE` at line 1353, `C14LEAN` at line 1459) in the same
order.

Fully-qualified names confirmed by namespace inspection:
- `FormalSystem.StarLanguage.StarAxiom` (`StarLanguage/Axioms.lean:133`, `inductive`)
- `FormalSystem.StarLanguage.StarDerivationTree` (`StarLanguage/Derivation.lean:80`, `inductive`)
- `FormalSystem.Metalogic.Conservativity.star_soundness_validIn` (`Star/StarSoundness.lean:133`)
- `FormalSystem.Metalogic.Conservativity.starDerivable_ofFormula_iff` (`Star/Forward.lean:134`)
- `FormalSystem.Metalogic.Conservativity.starConservative_of_plusComplete` (`Star/Forward.lean:171`)
- `FormalSystem.Metalogic.Conservativity.plusIncomplete_of_starNonconservative` (`Star/Forward.lean:183`)

## `Paper:` line audit for Phase 5 (already done, read-only)

Have one: `star_soundness_validIn`, `starDerivable_ofFormula_iff` — both read
`Paper: — (formalization-native; the manuscript supplies no proof system for `\BL^\star`)`.
Lack one: `StarAxiom`, `StarDerivationTree`, `starConservative_of_plusComplete`,
`plusIncomplete_of_starNonconservative`. This matches the plan's prediction exactly.

## What changed

- `FormalSystem/Semantics/StarValidity.lean` — `StarValidIn.of_forall_total` and
  `StarValidIn.apply_total` added to the `### Binder-shape adapters` block, immediately after the
  `StarValidOnFrames` pair and before `StarValid.of_forall_total`, mirroring
  `PlusValidIn.of_forall_total` / `.apply_total` line for line. `## Main Results` bullet now names
  the dotted forms explicitly.
- `FormalSystem/Metalogic/Conservativity/Star/StarSoundness.lean` — the local
  `starValidIn_of_forall_total` / `starValidIn_apply_total` and their
  `/-! ## The `StarValidIn` binder-shape adapters -/` section header deleted; all fourteen call
  sites repointed to the dotted names; a `## References` bullet added naming
  `Semantics/StarValidity.lean` as the adapters' home.

## Verification

- `bash .claude/scripts/lake-build-guard.sh build --timeout 1800 -- build
  FormalSystem.Metalogic.Conservativity.Star.StarSoundness` — exit 0,
  "Build completed successfully (1363 jobs)"; both changed modules built.
- `grep -rn 'starValidIn_of_forall_total\|starValidIn_apply_total' FormalSystem Tests docs scripts`
  returns nothing.
- No `sorry` in either changed file.

## Deviations

- No import cycle appeared, so the documented fallback branch was not taken.
- `StarSoundness.lean`'s `## Main Results` list never named the adapters and its docstring
  carried no prose siting them locally, so a `## References` bullet was added instead of editing
  Main Results.

## Concurrency warning

A concurrent session is editing `Conservativity/Plus/AxiomValidity.lean`,
`Independence/StabUndefinable.lean`, `PlusLanguage/Axioms.lean`, `Semantics/PlusNonValidities.lean`,
`Semantics/PlusTruth.lean` and `Semantics/StarNonValidities.lean` in this same working tree. Stage
by explicit path only, and commit each green sub-step immediately — this task lost a set of
uncommitted prose edits to that session once already.

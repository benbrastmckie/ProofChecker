# Phase 6 Handoff — Full gate and constraint sweep

**Status**: [COMPLETED]

## Immediate next action

None. All six phases are closed and the task is implemented. The orchestrator's postflight owns
the status transition.

## Gate results

- `bash .claude/scripts/lake-build-guard.sh build --timeout 1800 -- build` — exit 0,
  "Build completed successfully (2648 jobs)".
- `bash scripts/check-module-invariants.sh` — `ALL CHECKS PASSED`, exit 0.
- No `sorry` in live scope; every hit under `FormalSystem/` is in `Boneyard/` (archived) or is the
  word "sorry-free" in prose. This task's diff adds none.
- Axiom set byte-identical to the pre-task commit: `diff` over the 11 `^axiom ` lines under
  `FormalSystem/**.lean` is empty.
- No vacuous definition introduced; the one pre-existing tree-wide hit
  (`int_domain_universal`, `FormalSystem/Examples/TemporalStructures.lean`) is untouched.
- `bash .claude/scripts/check-task-references.sh` — `PASS: 0 unexempted task-reference
  occurrences across 4 tree(s)`; `PASS C9` inside the invariant script covers `FormalSystem/`,
  `lakefile.lean`, `README.md`, `scripts/`.
- `git diff` over the whole task on `scripts/check-module-invariants.sh` removes four lines, all
  comment prose or the `DECL` regex line being rewritten; no pre-existing `depends on axioms`
  baseline line altered. Both C14 heredocs carry 106 declarations in identical order.
- The three prose surfaces (`Soundness.lean` docstring, `Metalogic/README.md`,
  `StarLanguage/README.md`) state one consistent count: one schema (MF), two declarations
  (`modal_future_valid`, `mf_swap_valid`).

## The three files outside the declared TERRITORY, and why

The dispatch's TERRITORY named `Metalogic/Soundness.lean`, `Metalogic/README.md`,
`Semantics/StarValidity.lean`, `Metalogic/Conservativity/Star/StarSoundness.lean`,
`StarLanguage/README.md`, `docs/theorem-index.md`, `scripts/check-module-invariants.sh`. Beyond
those:

- `FormalSystem/StarLanguage/Axioms.lean`, `FormalSystem/StarLanguage/Derivation.lean`,
  `FormalSystem/Metalogic/Conservativity/Star/Forward.lean` — the plan anticipated these:
  deliverable (4) is unachievable without them, since C15's second assertion requires a `Paper:`
  line in each row's declaration's own doc comment. All edits are docstring-only.
- `scripts/module-invariants-allowlist.txt` — two entries so C5 stops reading two declaration
  names as module paths. This is the file's documented purpose.
- `FormalSystem/Metalogic/Conservativity/Star/README.md`,
  `FormalSystem/Metalogic/Conservativity/Plus/README.md`, `README.md` — regenerated inventory
  blocks, machine-generated, whose line counts this task's docstring edits moved.

## The corrected claim

`modal_future_valid` is **not** the sole consumer of time-shift homogeneity. It is one of two
declarations — with `mf_swap_valid` (`Metalogic/SoundnessLemmas/FrameClassVariants.lean`), the
`temporal_duality` companion that carries TF — both belonging to the single schema
`Axiom.modal_future`. The description's claim is right at the schema level and undercounts the
declarations by one. Both numbers are recorded.

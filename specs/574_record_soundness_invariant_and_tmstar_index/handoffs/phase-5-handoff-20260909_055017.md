# Phase 5 Handoff — TM⋆ ledger rows and declaration-site `Paper:` lines

**Status**: [COMPLETED WITH EXCLUSIONS]

## Immediate next action

Open Phase 6, the verification-only gate. It has nothing left to repair:
`bash scripts/check-module-invariants.sh` already exits 0 with `ALL CHECKS PASSED` on this exact
tree state.

## What changed

- `docs/theorem-index.md` — a `### TM⋆ over L⋆ — the store/recall language` section with **five**
  rows (`StarDerivationTree`, `star_soundness_validIn`, `starDerivable_ofFormula_iff`,
  `starConservative_of_plusComplete`, `plusIncomplete_of_starNonconservative`) and a prose lead-in
  recording why `StarAxiom` has no row; an L⋆/TM⋆ `Notation and naming` row; and, in the
  refutations section (retitled `## Statuses that are refutations, not gaps, and one status that is
  a gap`), a bullet recording TM⋆ completeness as OPEN and its entanglement with TM⁺ completeness.
- `FormalSystem/StarLanguage/Axioms.lean`, `FormalSystem/StarLanguage/Derivation.lean`,
  `FormalSystem/Metalogic/Conservativity/Star/Forward.lean` (two declarations) — `Paper:` lines
  added inside the existing `/-- … -/` blocks, wording copied verbatim from the two declarations
  that already carried one. No declaration, statement or proof line touched.
- `scripts/module-invariants-allowlist.txt` — two entries so C5 stops reading
  `FormalSystem.StarLanguage.StarAxiom` / `…StarDerivationTree` as module paths.
- `scripts/check-module-invariants.sh` — `inductive` added to C15's second-assertion `DECL`
  alternation.
- Regenerated inventory blocks in `FormalSystem/Metalogic/Conservativity/Star/README.md`,
  `FormalSystem/Metalogic/README.md` and `README.md` (line counts moved by Phases 3 and 5).

## The exclusion

`FormalSystem.StarLanguage.StarAxiom` gets no ledger row. `#print axioms` reports it as
`does not depend on any axioms`, which C14's `grep 'depends on axioms'` filter drops, so the
exact-string mechanism can pin nothing about it and this page admits no unpinned row. Full record
in the plan's `#### Reasoned Exclusions` table under Phase 5.

## Two files touched outside the Phase 5 file list

Both make the new rows pass the gate rather than change what the gate asserts:

- `scripts/module-invariants-allowlist.txt` — its documented purpose is exactly this.
- `scripts/check-module-invariants.sh` — `StarDerivationTree` is an `inductive` and C15's `DECL`
  alternation listed only `theorem|lemma|def|abbrev|instance`. Verified in isolation against the
  same 75 rows: the narrow regex fails on that one row, the wide regex passes all 75, and no other
  outcome changes. A row the widening newly finds is still required to carry its `Paper:` line, so
  it cannot silence a failure.

## Verification

`bash scripts/check-module-invariants.sh` — `ALL CHECKS PASSED`, exit 0. Notable lines:
`PASS C1` (both), `PASS C2`, `PASS C5 (6 allowlisted)`, `PASS INV`, `PASS C9`, `PASS C14` (both),
`PASS C15` (both; 58 anchors, all 75 rows anchored), `PASS C21`.

# Phase 4 Handoff — Measure axiom sets and extend the C14 baseline pair

**Status**: [COMPLETED]

## Immediate next action

Open Phase 5: five ledger rows (not six) in `docs/theorem-index.md`, plus the `Paper:` lines at
the four declarations that lack them, plus the TM⋆ completeness open-status bullet and the L⋆/TM⋆
notation row.

## The measurement (raw, through the exact C14 pipeline)

`lake env lean` + the continuation-line rejoin `sed`, before the `grep 'depends on axioms'` filter:

```
'FormalSystem.StarLanguage.StarAxiom' does not depend on any axioms
'FormalSystem.StarLanguage.StarDerivationTree' depends on axioms: [propext]
'FormalSystem.Metalogic.Conservativity.star_soundness_validIn' depends on axioms: [propext, Classical.choice, Quot.sound]
'FormalSystem.Metalogic.Conservativity.starDerivable_ofFormula_iff' depends on axioms: [propext, Classical.choice, Quot.sound]
'FormalSystem.Metalogic.Conservativity.starConservative_of_plusComplete' depends on axioms: [propext, Classical.choice, Quot.sound]
'FormalSystem.Metalogic.Conservativity.plusIncomplete_of_starNonconservative' depends on axioms: [propext, Classical.choice, Quot.sound]
```

**`StarAxiom` is not C14-pinnable.** Its line does not survive the `grep 'depends on axioms'`
filter, so it contributes to neither heredoc and the exact-string comparison cannot assert
anything about it. Fabricating a baseline line for it would be a hard C14 failure. It is
therefore absent from both heredocs and gets **no ledger row** — the plan's own contingency. C2
offers no escape: it applies the identical filter.

**`StarDerivationTree` IS pinnable** at the literal `[propext]` — the plan's prediction that both
`inductive`s would drop out was half wrong, and the correction is in the plan's favour.

## Axioms-column values for Phase 5's rows

| Declaration | Axioms cell |
|---|---|
| `FormalSystem.StarLanguage.StarDerivationTree` | `` `[propext]` pinned:C14 `` |
| `FormalSystem.Metalogic.Conservativity.star_soundness_validIn` | `pcq pinned:C14` |
| `FormalSystem.Metalogic.Conservativity.starDerivable_ofFormula_iff` | `pcq pinned:C14` |
| `FormalSystem.Metalogic.Conservativity.starConservative_of_plusComplete` | `pcq pinned:C14` |
| `FormalSystem.Metalogic.Conservativity.plusIncomplete_of_starNonconservative` | `pcq pinned:C14` |

`` `[propext]` `` written out literally follows the existing `plusValidIn_ofFormula_iff` row.

## What changed

`scripts/check-module-invariants.sh` only:
- five lines appended to the `C14BASE` heredoc and five matching `#print axioms` lines appended to
  the `C14LEAN` heredoc, in the same order, at the end of each;
- the "Seven entries carry a STRICT SUBSET" comment updated to eight, naming
  `StarLanguage.StarDerivationTree`;
- a new comment paragraph recording that the trailing block pins the TM⋆ ledger results and that
  `StarAxiom` is deliberately absent, with the reason.

No pre-existing baseline line was modified. `git diff` shows three deleted lines, all of them
comment prose being rewritten.

## Verification

- `bash scripts/check-module-invariants.sh`:
  - `PASS  C1   lake build exits 0`
  - `PASS  C1   lake build BimodalTest exits 0`
  - `PASS  C2   all four flagship axiom sets match baseline`
  - `PASS  C14  every pinned declaration matches its axiom baseline`
  - `PASS  C15` (both assertions; 58 anchors, 67 rows)
  - `PASS  C21  all 27 declaration(s) named in MainResults.lean are pinned by C2 or C14`
  - `PASS  C9   zero task-number citations under FormalSystem/, lakefile.lean, README.md, scripts/`
- Heredoc parity: 106 declarations on each side, identical order (`diff` clean).

## Outstanding red — the whole-tree gate, not this phase

`check-module-invariants.sh` exits 1 on the INV group alone:
- `FormalSystem/Semantics/README.md: PlusStateLocal.lean is live but has no row` — a concurrent
  session's file (commit 673e6be3e), outside this task's territory.
- Stale generated blocks in `FormalSystem/Metalogic/Conservativity/Star/README.md`,
  `FormalSystem/Metalogic/README.md`, `README.md` — line counts moved by Phase 3 and by the
  concurrent session. Regenerate with `--emit-inventory` at the end of Phase 5, after Phase 5's
  `Paper:` lines have moved those counts again.

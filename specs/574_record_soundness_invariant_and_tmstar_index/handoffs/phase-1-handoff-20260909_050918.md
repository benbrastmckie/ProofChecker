# Phase 1 Handoff — Audit the time-shift consumer set and record the invariant

**Status**: [COMPLETED]

## Immediate next action

Open Phase 3 (relocate the `StarValidIn` binder-shape adapters). Phase 2 depends on Phase 1 and
is also eligible; Phase 3 is cheaper to close (two theorem moves, one scoped build) and is
already fully scouted, so cheapest-closure-first selects Phase 3.

## Audit result (the load-bearing finding)

Direct consumers of `Semantics.TimeShift.timeShift_preserves_truth`, live tree, Boneyard excluded:

**Soundness layer (axiom/rule validity) — two declarations, one schema:**
- `modal_future_valid` — `FormalSystem/Metalogic/Soundness.lean:303`
- `mf_swap_valid` — `FormalSystem/Metalogic/SoundnessLemmas/FrameClassVariants.lean:105`
  (the `temporal_duality` companion; TF is not a separate `Axiom` constructor, so TF's validity
  rides on this one)

Both belong to the single constructor `Axiom.modal_future`. The description's "sole consumer"
claim is therefore correct at the *schema* level and an undercount by one at the *declaration*
level. The docstring states both numbers.

**Soundness module but not a schema-validity proof — one declaration:**
- `minusTruthAt_timeShift` — `FormalSystem/Metalogic/Conservativity/MinusLanguageSoundness.lean:166`
  (L⁻ restatement of time-shift homogeneity itself; currently no consumer)

**Non-soundness consumers:** `Semantics/Truth.lean` (`timeShift_preserves_truth_total`,
`exists_shifted_history`, `box_const`), `Semantics/ShiftSet.lean` (`reverse_repr`),
`Metalogic/Decidability/Verified/Decidable.lean` (`truthAt_allFuture_of_box`,
`truthAt_allPast_of_box`, `forall_truthAt_time_invariant`),
`Metalogic/Decidability/BiLasso/BoxOracle.lean` (`boxOracle_sound`),
`Metalogic/Decidability/Verified/Bridge/RegionFrame.lean` (`truthAt_box_iff`,
`truthAt_regionHistory_offset`).

**No other object language re-proves MF from time shift.** L⁻ discharges it proof-theoretically
(`MinusLanguage/AxiomDischarge.lean`, `dischargeModalFuture`); L⁺ transfers it
(`Metalogic/Conservativity/Plus/Atomization.lean`'s `plusValidIn_of_tm` /
`plusValidIn_swap_of_tm`, applied in `Plus/AxiomValidity.lean`); the coarsened independence
models transfer it (`Metalogic/Independence/CoarsenedModels.lean`, `cValid_of_tm`); L⋆ does not
have it (`refute_modal_future`).

`timeShift_preserves_truth_total` has no consumer anywhere in the live tree.

## What changed

- `FormalSystem/Metalogic/Soundness.lean` — module docstring only. New `## The time-shift
  consumer set` section (between the Totality Parameterization block and `## Full Derivation
  Soundness`), and the `**Key Techniques**` time-shift bullet rewritten from "(MF, TF)" to
  "(MF, and TF through it)" with a pointer to the new section.

## Verification

- `bash .claude/scripts/lake-build-guard.sh build --timeout 1800 -- build
  FormalSystem.Metalogic.Soundness` — exit 0, "Build completed successfully (1344 jobs)".
- Diff confined to lines 65 and 78-139, both inside the `/-! … -/` module docstring (lines 14-208).
- Every declaration name and file path cited in the new prose was grep-verified to resolve.

## Deviations

- Phase 1's conditional "if the count is greater than one" item: the count is greater than one at
  the declaration level but not at the schema level, so the docstring records both readings rather
  than a single number.

## Count for Phase 2 to propagate

Two declarations, one schema. `Metalogic/README.md` and `StarLanguage/README.md` must both agree
with that, and neither may assert "sole consumer" without saying which level it means.

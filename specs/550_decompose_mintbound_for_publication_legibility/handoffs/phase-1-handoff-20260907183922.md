# Phase 1 handoff — task 550

**Next action**: Phase 2 — drop `private ` from the 16 named declarations in MintBound.lean, then
detached+guarded `lake build FormalSystem`.

**Baseline (scratchpad `/tmp/claude-1000/.../scratchpad/`)**:
- HEAD 6148d238ef5db928e3960df8ce8ea187489b4a11; MintBound.lean unmodified
- 15,684 lines; 751 declarations (all names unique); 92 `private`
- 0 structural `sorry` (one prose mention at :15616, inside the C9 register)
- 25 `set_option`, all per-declaration `... in` form (23 maxHeartbeats, 2 linter.unusedTactic)
- Single `namespace` (:57) and single `open` (:59); no `variable`
- Harness: `decls.py` (name/private/full extraction, comment-stripped), `xprivate.py`
  (cross-boundary private), `partition.txt` (18 spans)

**Confirmed**: all 18 partition boundaries land on existing `/-!` headers; spans gapless
61-15,682; all 5 `section ... end` blocks contained; cross-boundary private check returns exactly
the plan's 16 names at exactly the plan's line numbers.

**Deviation**: plan's "five `attribute [local simp]` blocks" measured as three; all three sit
inside contained sections, so the conclusion is unchanged.

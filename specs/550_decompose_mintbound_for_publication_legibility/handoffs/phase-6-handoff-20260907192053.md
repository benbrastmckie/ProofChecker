# Phase 6 + 7 handoff — task 550

**Next action**: Phase 8 — `check-module-invariants.sh`, `lake exe checkInitImports`, final full
build, baseline diff, count reconciliation, then the implementation summary.

**State**: all 18 modules extracted. `MintBound.lean` is a 121-line aggregator: license, 18
imports, a `## Submodules` map, zero declarations. `lake build FormalSystem` GREEN (2610 jobs).
Declaration-name set identical (751); cross-boundary private 0; missing-import violations 0;
axiom set identical (41 rows). `Decidability.lean` shows a zero-line diff across the whole task.
Largest module `MintPotential.lean` 1,828 lines (was one file of 15,684).

**Provenance audit, final**: only 26 snapshot lines are absent from the split tree — 18 promoted
module headings, 6 promoted sub-headings inside those same docstrings, and 2 aggregator-docstring
lines deliberately rewritten in Phase 6 ("This module supplies that bound in four blocks" and a
"below" that no longer had a referent). Every declaration and every proof line is accounted for.

**READMEs written** (Phase 7): `MintBound/README.md` (new, 18-row table, import DAG, the
register-vs-live-refutation convention) and `Termination/README.md` (the stale 14,770 row replaced
by the 121-line aggregator row, plus a `## MintBound/` section).

**Known deviation to report at Phase 8**: the plan asserts the finished aggregator is "under 100
lines". It is 121 — the `## Submodules` map with 18 entries plus the retained A/B/C/D overview
does not fit under 100, and the overview is the file's orientation. Measured, not absorbed.

# Phase 3 handoff — task 550

**Next action**: Phase 4 — `python3 <scratch>/extract.py Measure Terminus ClosureResidual TimeCensus`,
then verify-split.py + provenance.py, then the build gate.

**State**: 4 of 18 modules extracted (`Register` 915, `Invariants` 1021, `OrderingTimes` 1016,
`MintPotential` 1828 lines); residual `MintBound.lean` 10,952 lines. `lake build FormalSystem`
GREEN (2596 jobs). Declaration-name set identical to baseline (751); cross-boundary private 0;
missing-import violations 0; axiom set identical (41 rows). Register carries all 25 entries with
only the header's "Twenty-four" -> "Twenty-five" corrected.

**Provenance audit**: the ONLY content difference between the split tree and the pre-split
snapshot is the four promoted `##` -> `#` module headings. Everything else added is license
blocks, imports, `namespace`/`open`/`end`.

**Tooling** (scratchpad): `source-snapshot.lean` is the frozen post-Phase-2 source every later
extraction cuts from — do not re-derive it from the tree. `extract.py <Module>...` writes the
named modules and rewrites the residual; `verify-split.py` runs checks (a)-(d);
`provenance.py` runs the line-multiset audit.

**Scheduling deviation**: Phase 7 (READMEs) is deferred to run after Phase 6 rather than in
parallel with Phase 4, so that every module path it cites already resolves and no intermediate
commit can leave C5/C12/C13 red.

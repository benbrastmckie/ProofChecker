# Phase 2 handoff — task 550

**Next action**: Phase 3 — extract `Register`, `Invariants`, `OrderingTimes`, `MintPotential`
via the scratchpad `extract.py`, then the build gate.

**State**: `private ` dropped from exactly the 16 planned declarations. Declaration-name set
identical to baseline (751); private 92 -> 76; cross-boundary private check now returns empty.
`lake build FormalSystem` GREEN (2592 jobs). Axiom baseline captured (41 `#print axioms` rows)
at scratchpad `axioms-baseline.txt`.

**Pre-existing repo failures recorded before any of my edits** (`--no-build` invariants pass):
`INV` (2 stale generated inventory blocks, from concurrent work) and `C9` (one task-number
citation at MintBound.lean:12413). Neither is caused by this task; C9's site is inside the range
Phase 3-6 relocates, and is fixed as prose hygiene during Phase 3.

**Three C20 landmines found, not in the plan**: MintBound.lean self-cites `MintBound.lean:1260`
(once) and `MintBound.lean:1071` (twice) in prose. The aggregator will be ~40 lines, so all three
go out of range and C20 tier 1 (repo-wide GATED) fails. Fixed as same-line prose edits in
Phase 3 so no line number shifts.

**Guard invocation correction**: `lake-build-guard.sh build ... -- FormalSystem` (the plan's form)
exits 77 without building. Correct form: `... -- build FormalSystem`, exit code captured directly.

# Phase 9 handoff — every provably-wrong citation fixed

**Next action**: Phase 10 — convert the 161 publication-scope citations to declaration names and
turn on `ENFORCE_C20=1`.

**State**: C20 tier 1 is clean — all 1,174 resolvable citations land on a real, non-blank line.
The widened C9 is clean. `bash scripts/check-module-invariants.sh --no-build` exits 0 again.
116 defective citations across 58 files had their line number stripped; 24 range artifacts
(`Foo.lean-54`, left by stripping the start of `Foo.lean:49-54`) were repaired in the same pass;
17 ephemeral `specs/<n>_<slug>/` citations across 12 files were rewritten to name the artifact
by its stable filename, and `scripts/check-evidence-probes.sh` was excluded from C9 because the
path it names is the directory it reads, not a citation.

**Verified**: every added line in a changed `.lean` file lies inside a comment region — checked
mechanically, and the only exceptions belong to the concurrent session, not to this task.

**Not yet built**: 62 `.lean` files changed (docstrings only). A full `lake build` has not run
since. Phase 10 or 11 should absorb that cost.

**Concurrency**: the parallel task-539 session edited `Automation/**`, `BiLasso/Assembly.lean`,
`Theorems/DiscreteUnfolding.lean` and `scripts/nolints.json` during this phase; those were
deliberately left unstaged.

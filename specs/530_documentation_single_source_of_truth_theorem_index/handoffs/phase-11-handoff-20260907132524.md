# Phase 11 handoff — three-register pass A

**Next action**: Phase 12 — the same pass over `StrongCompleteness.lean`, `SetConsequence.lean`
and `Conservativity.lean`'s duplicated ledger rows.

**State**: `Semantics/Validity.lean` 999 -> 944 lines, archaeology hits 7 -> 0; the false claim
that `SemanticConsequence` has a binder list is corrected (it binds no `D`; the class to match is
`satisfiable`'s); the `def:logical-consequence` verbatim quote is stated once, at the declaration,
with a pointer from the module docstring; the 88-line `ValidComplete` caveat stated its
`soundness_rtime` trap three times and now states it once; the stale
"this predicate's binders are the paper's TM⁺_c" claim is corrected — TM⁺_c is dense-and-complete,
which is `FrameClass.RTime`. `FrameClassValidity.lean` 199 -> 191, with the two rejected
relocations extracted to
`specs/530_.../extracts/adr-008-frameclass-validity-seam-body.md` for Phase 13 and a pointer
left in the docstring. Both modules type-check (`lake env lean`), and
`check-module-invariants.sh --no-build` exits 0 with C19 at 92.34%.

**C20 earned its keep immediately**: shortening `Validity.lean` broke a citation to
`Semantics/Validity.lean:255` from `WeakCanonical/PriorDefsDense.lean`, C20 tier 1 named it, and
it was fixed in the same phase. That is precisely the failure mode the convention exists for.

**Dangling pointer until Phase 13**: `FrameClassValidity.lean` now cites
`docs/architecture/ADR-008-frameclass-validity-seam.md`, which does not exist yet. Phase 13
creates it from the extract.

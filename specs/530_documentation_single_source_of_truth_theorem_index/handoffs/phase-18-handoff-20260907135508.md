# Phase 18 handoff — publication packaging

**Next action**: Phase 19 — `## Tags` lines. It is the last phase.

**State**: four artifacts exist.
- `CITATION.cff` — `year: 2026` throughout, with the paper as a `references` entry. The
  ProofChecker/BimodalLogic name split is **removed** rather than explained: "ProofChecker"
  survived only as a stale BibTeX key and title, so there are no longer two names.
- `references.bib` — 13 entries, each cited by name somewhere in the tree (Burgess 1982,
  Kamp 1968, Reynolds 1992/1994, Doets 1987, Blackburn–de Rijke–Venema 2002, Prior 1967,
  Ćmiel–Kuhlmann–Kuhlmann 2021 for the Saturation ball-space footnote, and the rest).
- `docs/ARCHITECTURE.md` — one layer diagram with **both** upward edges drawn *inside* the
  diagram body, labelled UPWARD EDGE (1) (`Semantics -> ProofSystem` via
  `FrameClassValidity.lean`) and (2) (`Decidability -> Automation`), each with its own
  explanatory section. Layer 0 is given in full, `ForMathlib` and `StarLanguage` included.
- `README.md` `## Verifying the main theorems` — a five-declaration `#print axioms` snippet plus
  the invariant-script one-liner. **The snippet was extracted from the README and run verbatim**;
  it produces exactly the output the section documents. The section also states the real pinned
  count (105) rather than implying the five are all of them.

`check-module-invariants.sh --no-build` exits 0, `readme-lint.sh` exits 0.

**Note on edge (2)**: `Decidability -> Automation` is a *data-flow* edge, not an import edge —
the import runs `Automation -> Decidability`, which is downward and unremarkable. The diagram
says so explicitly, because conflating the two graphs is the easy mistake here.

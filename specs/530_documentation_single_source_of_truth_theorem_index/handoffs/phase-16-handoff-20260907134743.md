# Phase 16 handoff — readme-lint green

**Next action**: Phase 17 — generate the typst per-declaration axiom table.

**State**: `bash scripts/readme-lint.sh` **exits 0** — 0 missing READMEs, 0 broken references,
0 missing dates, across 52 READMEs. The five missing ones were written:
`ForMathlib/Order/`, `Metalogic/Conservativity/`, `Metalogic/Conservativity/Star/`,
`Semantics/Frames/`, `Semantics/Ultraproduct/`, each with a generated inventory block, key
results, cross-links and a stamp. 29 stamps refreshed and 18 added.
`SoundnessLemmas.lean` now imports `DiscreteOrder` and lists it in Contents; a full
`lake build` succeeds (2591 jobs) with that import added, and
`check-metalogic-cycles.sh` still reports exactly one cycle.
`check-module-invariants.sh --no-build` exits 0.

**Standing tension worth recording**: `Last verified` stamps are hand-maintained metadata of
exactly the kind this task exists to remove, and each goes stale on the next edit to its
directory. `readme-lint.sh` reports staleness rather than gating it, which is the right
treatment — but a future task should consider deriving the stamp from git rather than typing it.

**Found for Phase 17**: `bash scripts/typst-sync-check.sh` currently FAILS on one violation —
`typst/chapters/03-proof-theory.typ` cites `co_derived`, which the concurrent task-539 rename
turned into `coDerived`. That is Phase 17's territory (typst) and must be fixed there.

# Phase 10 handoff — publication scope clean, C20 enforced

**Next action**: Phase 11 — the docstring three-register pass over `Validity.lean` and
`FrameClassValidity.lean`.

**State**: C20 tier 2 reports zero; `ENFORCE_C20` now defaults to **1**, so a new `file:line`
citation on any publication-facing surface fails the gate. 161 citations across 25 files had
their line number stripped, and 66 bare `` `:NNN` `` orphans were removed in the same pass.
`docs/reference/API_REFERENCE.md`'s header is refreshed (Version 1.2.0, dated, status honest)
and now points at `docs/theorem-index.md` for per-theorem status.
`bash scripts/check-module-invariants.sh --no-build` exits 0 with every check green.

**Rejected approach, recorded so it is not retried**: automated conversion of a citation to the
declaration name at the cited line. It duplicates names the prose already gives, cannot tell
backtick context, and guesses whenever the cited line sits in a module docstring. Stripping the
line number is the correct mechanical operation; naming is a per-site editorial judgement.

**Not yet built**: no `lake build` has run since Phase 9. Eleven `.lean` files changed here
(comment regions only, verified mechanically). Phase 11 must absorb the build.

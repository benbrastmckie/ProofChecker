# Phase 15 handoff — verifiable-mismatch sweep

**Next action**: Phase 16 — README completeness and stamps (`readme-lint.sh` to zero).

**State**: every mismatch on the phase's list is fixed.
- `Semantics.lean`: the frame table now names the **four** axioms of `def:frame`
  (Compositionality, Seriality, Limit, Saturation) with Nullity stated as derived; the truth-clause
  table shows the real four-argument `TruthAt`, the `□` clause reads `σ.IsTotal` (not
  `σ.domain t`), the non-existent `H`/`G` clauses are replaced by the real `untl`/`snce` clauses,
  and the `#check` example compiles.
- `Soundness.lean`: the `Set.univ` claim is gone — `TruthAt` takes no set argument.
- `Semantics/Truth.lean`: the pointer to a module-hierarchy detail `SoundnessLemmas.lean` does
  not carry is replaced by a statement of where the bridge theorems actually live.
- `SoundnessLemmas/FrameClassVariants.lean`: the "resolves the 3 `temporal_duality` sorries"
  claim with three wrong line numbers is replaced by what the lemmas are actually for; C3 asserts
  the sorry inventory is zero.
- `Automation/AesopRules.lean`: TL/MF/TF are no longer described as "pending soundness proofs" —
  their soundness is proved; the exclusion is a search-space decision.
- `README.md`: `cd BimodalLogic`, and the software BibTeX entry renamed/retitled/re-dated so key,
  title and repository name agree.
- `docs/README.md`: the `lake build :docs` recipe is removed — `doc-gen4` is not a dependency.
- **42** `Bimodal.*` references (not 26) across 14 READMEs rewritten to `FormalSystem.*`, which
  also brings them inside C5's scope; all 42 resolve.

`bash scripts/check-module-invariants.sh --no-build` exits 0. `Semantics.lean` and
`Semantics/Truth.lean` type-check individually.

**Owed**: a full `lake build`. Five `.lean` files changed here and the tree has not had a clean
build since Phase 9; `MintBound.olean` is currently absent. Phase 16's verification tier is
`full`, so it must absorb this.

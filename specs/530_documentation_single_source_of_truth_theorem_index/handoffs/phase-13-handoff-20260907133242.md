# Phase 13 handoff — four ADRs

**Next action**: Phase 14 — the sentence-level shingle pass in C18.

**State**: `docs/architecture/ADR-005-Single-Boneyard.md`,
`ADR-006-Metalogic-No-Physical-Regroup.md`, `ADR-007-Decidability-One-Directional.md` and
`ADR-008-FrameClass-Validity-Seam.md` exist and are listed in the catalog and detail sections of
`docs/architecture/README.md`, which also now states explicitly that this directory is the one
ADR convention and that there is no `docs/decisions/`. Each rationale appears exactly once:
the archive-consolidation narrative only in ADR-005, the declined regroup only in ADR-006, the
`validity_decidable` retirement argument only in ADR-007 (the three sites that merely *cite*
`Correctness.lean`'s retirement record were left alone — they are pointers, not copies), and the
two rejected relocations only in ADR-008. `README.md`, `FormalSystem/README.md` and
`FormalSystem/Metalogic/README.md` carry pointers where the narratives used to be.
`bash scripts/check-module-invariants.sh --no-build` exits 0.

**C12 caught the one hazard**: ADR-005 quoted the retired nested-archive path, which no longer
resolves. Rewritten in prose rather than as a path.

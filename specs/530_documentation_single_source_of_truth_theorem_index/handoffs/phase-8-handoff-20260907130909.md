# Phase 8 handoff — C20 and the widened C9

**Next action**: Phase 9 — fix every provably-wrong citation so C20 tier 1 and C9 go green.

**State**: C20 exists with two tiers. Tier 1 (gated, repo-wide) reports **118** citations whose
target line does not exist or is blank — 29 out of range, 89 blank. Tier 2 reports **180**
citations in publication-facing scope across **29** files and flips to FAIL under
`ENFORCE_C20=1` (verified both ways); `Metalogic/WeakCanonical/**` is excluded from tier 2 only.
64 citations name an ambiguous or archived filename and are reported unverifiable, never failed.
C9 and C9D now also match `specs/<number>_<slug>/` paths; C9 reports **14** and C9D **142**.
The standing conventions are recorded in a new section at the top of `FormalSystem/README.md`.

**The gate is deliberately RED after this phase**: C9 (14) and C20 tier 1 (118). Phase 9 clears
both. Do not flip a flag to quiet them.

**Phase 9 note**: one of C9's 14 hits is `scripts/check-evidence-probes.sh:28`, where the
`specs/.../evidence` path is a functional path the script reads, not a citation. It needs an
exclusion or a relocation decision, not a text edit.

**Watch out**: writing about these conventions in a checked file makes the checks fire on your
own prose. `FormalSystem/README.md`'s convention section had to be phrased with
`Foo.lean:<line>` and `specs/<number>_<slug>/` for exactly that reason.

# Phase 4 handoff — C14 axiom baseline extended

**Next action**: Phase 5 — seed `docs/theorem-index.md`.

**State**: `C14_BASELINE` grew from 52 to 99 rows; with C2's four that is **103 machine-pinned
declarations**. Every subject of a SORRY-FREE claim in `FormalSystem/Metalogic.lean` is now
pinned — the docstring-minus-pinned difference is empty, asserted by re-running the extractor in
the phase's verification. A full `bash scripts/check-module-invariants.sh` (with build) exits 0.

**Recorded for Phase 5**: `specs/530_.../pinned-axioms.tsv` carries
`<fully-qualified name>\t<pcq | literal list>\tpinned:C2|C14` for all 103, so the index's Axioms
column needs no second build.

**Findings**: the re-derived unpinned set was 47, not the report's 35. Seven declarations carry a
strict subset of `[propext, Classical.choice, Quot.sound]`:
`setConsequence_of_not_satisfiable`, `satisfiableSet_iff_finitelySatisfiable`,
`modelExistence_iff_finitelySatisfiable`, `Conservativity.TMFrag`,
`Semantics.starValidIn_ofFormula_iff`, `Semantics.galoisClosed_mod` are `[propext]`;
`qDepth_qAlpha` is `[propext, Quot.sound]`.

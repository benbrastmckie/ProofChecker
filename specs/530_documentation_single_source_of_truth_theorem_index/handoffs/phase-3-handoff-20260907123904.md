# Phase 3 handoff — Metalogic census deleted

**Next action**: Phase 4 — extend the `C14_BASELINE` axiom pin set.

**State**: `FormalSystem/Metalogic.lean`'s 34-line Module Structure census is replaced by four
sentences plus pointers; the two-Boneyards and Kamp-Boneyard claims are gone; the dangling
"instantiating the reductions / the single reduction" fragment is repaired; the ledger copy at
the Conservativity entry now points at `docs/theorem-index.md` instead of restating a stale
"two countermodels remain outstanding". `Metalogic/README.md`'s "**Ten** loose files" prose is
replaced (the table itself is generated, so its five phantom rows went in Phase 1).
`bash scripts/check-module-invariants.sh --no-build` exits 0.

**Naming warning for Phases 4-6**: the tree uses `ztime`/`rtime`, not the `discrete`/`dedekind`
names the plan and research report use — e.g. `completeness_ztime`, `notCompactZTime`,
`notStrongCompletenessRTime`, `sat_rtime_ssubset_mod_axiomSet`. Derive every declaration name
from the tree, never from the plan.

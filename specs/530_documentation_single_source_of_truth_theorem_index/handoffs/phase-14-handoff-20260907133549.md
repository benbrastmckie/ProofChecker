# Phase 14 handoff — C18 sentence pass

**Next action**: Phase 15 — the verifiable-mismatch sweep.

**State**: C18 now reports at two granularities and both read zero. The paragraph pass is
retained unchanged; the new pass shingles on sentence boundaries at a 15-word floor over the
same four files. Negative-tested: reintroducing one removed sentence makes it fire and name both
sites.

**Finding worth carrying forward**: the sentence pass sees only one of the two duplicates the
research report named. The other differs in its sentence opening, so no shingle matches — a
near-duplicate below the detector's resolution. The detector is a floor, not a proof of absence;
duplication of *claims* still needs a reader. The fix removed the whole duplicated Galois essay
from `Metalogic.lean`, which now carries the SORRY-FREE claim (C14 pins it) plus pointers to
`README.md`'s exposition and `docs/theorem-index.md`'s status.

**Stale claim fixed in passing**: `Metalogic.lean` described Dedekind strong completeness as
"unresolved"; `notStrongCompletenessRTime` refutes it.

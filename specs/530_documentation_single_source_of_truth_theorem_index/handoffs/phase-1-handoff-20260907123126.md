# Phase 1 handoff — inventory generator mechanism

**Next action**: Phase 2 — convert the remaining eight registered inventory targets.

**State**: `scripts/check-module-invariants.sh --emit-inventory[ --check]` exists and is wired
into the main run as check `INV`. `scripts/lib/live_walk.py` is the single Boneyard-excluding
walk, imported by both the C4-C11 heredoc and the generator.
`FormalSystem/Metalogic/README.md` is converted (five blocks) and generation dropped its five
phantom loose-file rows and added `Conservativity.lean` / `Conservativity/`.
`bash scripts/check-module-invariants.sh --no-build` exits 0.

**Key decisions**: marker options `dir= rows= filter= cols= desc= link= sort=`; the trailing
Description column is hand-written and carried across by key; fenced code blocks are skipped so
documentation of the marker is not itself treated as a target.

**Deviations**: recorded inline on the Phase 1 checklist items in the plan.

**Remaining targets for Phase 2**: `README.md`, `FormalSystem/README.md`,
`FormalSystem/Automation/README.md`, `FormalSystem/Syntax/README.md`,
`FormalSystem/Theorems/README.md`, `FormalSystem/Metalogic/SoundnessLemmas/README.md`,
`FormalSystem/Metalogic/Independence/README.md`, `FormalSystem/Automation/Tactics/README.md`.

# Phase 12 handoff — three-register pass B

**Next action**: Phase 13 — the four ADRs under `docs/architecture/`.

**State**: archaeology is at **zero** across `StrongCompleteness.lean` (1,141 -> 1,127) and
`SetConsequence.lean` (626 -> 616); "before this collapse there were four byte-identical
definitions" and "pre-collapse binder shape" are gone, replaced by present-tense caller traps.
`Conservativity.lean` was not halved — by design — but its H/G-fragment and L⋆ paragraphs no
longer enumerate per-theorem status; they point at `docs/theorem-index.md` and keep the
reasoning. All three modules type-check; `check-module-invariants.sh --no-build` exits 0.

**C20 caught a second shifted citation** — `WeakCanonical/RealModel/README.md` cited
`StrongCompleteness.lean:469`, which shortening the file made blank. Fixed in the phase. Expect
one of these after every prose pass over a heavily-cited module.

**Phase 13 input**: `specs/530_.../extracts/adr-008-frameclass-validity-seam-body.md` holds the
two rejected relocations extracted from `FrameClassValidity.lean` in Phase 11, and
`FrameClassValidity.lean` already points at `docs/architecture/ADR-008-frameclass-validity-seam.md`,
which does not exist yet.

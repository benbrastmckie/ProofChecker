# Phase 6 handoff — paper anchors at the flagship declarations

**Next action**: Phase 7 — extend C15 with the theorem-index assertion.

**State**: all **52** `docs/theorem-index.md` rows carry a `Paper:` line in the declaration's own
`/--` block — 16 real anchors (`thm:TM-soundness`, `cor:tm-completeness`, `app:dense`,
`app:discrete`, `app:complete`, `app:deterministic`) and 36 `Paper: — (reason)` lines.
`lake build` is green (2591 jobs); `check-module-invariants.sh --no-build` exits 0 and C15 now
resolves 53 anchors. C19 coverage 92.35%, no regression.

**Tooling left behind** (scratchpad, not committed):
`add_anchors.py` (idempotent, comment-aware inserter) and `verify_anchors.py` (asserts every
index row's declaration carries its anchor or a dash-plus-reason). Phase 7's C15 assertion should
implement the same rule the verifier does.

**Build cost warning**: `Decidability/Verified/Termination/MintBound.lean` is 15,252 lines and
elaborates in **252 s**; a full rebuild after any upstream docstring edit takes ~25 minutes wall
clock. Batch Lean-file edits into as few build cycles as possible.

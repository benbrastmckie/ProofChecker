# Phase 5 handoff — docs/theorem-index.md seeded

**Next action**: Phase 6 — add a one-line `Paper:` anchor to each flagship declaration.

**State**: `docs/theorem-index.md` exists: 52 rows in 13 sections, every Lean name fully
qualified and machine-verified to resolve, every File cell an existing path with no `:NNN`, every
Axioms cell drawn from the C2/C14 baselines (no row reads `claimed`). It carries a
Notation-and-naming table mapping paper term to Lean identifier, and a "statuses that are
refutations, not gaps" section. Pointers to it were added to `README.md`,
`FormalSystem/README.md`, `FormalSystem/Metalogic/README.md` and
`FormalSystem/Metalogic/Conservativity.lean`; the README's L/L⋆ ledger is down to five rows.
`bash scripts/check-module-invariants.sh` (full, with build) passes C1/C2/C14.

**Phase 6 site list** (derive from the index, per the plan): 22 files carry index rows. The
largest groups are `StrongCompleteness.lean` (8), `Conservativity/Backward.lean` (5),
`Conservativity/BaseLanguageSoundness.lean` (5), `Compactness.lean` (4), `Soundness.lean` (4),
`Conservativity/Fragment.lean` (4), `BXCanonical/Completeness.lean` (3).

**Anchors available**: `thm:TM-soundness` (soundness rows), `cor:tm-completeness` (weak
completeness), `app:dense` / `app:discrete` / `app:complete` (correspondence),
`app:deterministic`. Compactness, non-compactness, consequence completeness, conservativity and
expressiveness rows carry `—` with a one-clause reason; that is the deliverable, not a gap.

**Watch out**: C5 treats any dotted, all-capitalised `FormalSystem.*` string in markdown as a
module path. Write declaration names whose components are capitalised (`FrameClass.Sat`) without
the `FormalSystem.` prefix, or C5 fails.

# Phase 7 handoff — C15 second assertion

**Next action**: Phase 8 — the two-tier C20 citation check and the C9 regex widening.

**State**: C15 now reports two independent halves. The resolution half resolves 53 anchors; the
new half asserts all 52 `docs/theorem-index.md` rows carry their anchor, or `Paper: — (reason)`,
in the declaration's own `/--` block, and additionally that each File cell is an existing path
with no line number. Negative-tested: swapping one row's anchor makes it fail and name that row.
`bash scripts/check-module-invariants.sh --no-build` exits 0.

**Correction to the plan's premise**: C15 was NOT red. `app:drift`, `cor:no-characterization`
and `lem:deterministic-singleton` are recorded LIVE-UNPINNED in the record's KNOWN-ANCHORS block,
so risk R2 and the corresponding non-goal are moot and no user decision is required.

**Concurrency warning**: another session (task 539, linter debt) is committing to `main` in
parallel and has over-staged into this task's files at least once. Stage explicit file lists,
never a directory, and re-check `git log --oneline -3` before every commit.

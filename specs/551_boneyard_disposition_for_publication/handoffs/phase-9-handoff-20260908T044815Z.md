# Handoff — task 551, after Phases 1-4 and during Phase 9

## Immediate next action

Phase 9's two edits are applied and the inventory regenerated; the guarded `lake build` is the
only outstanding verification. Once it is green, close Phase 9 and open **Phase 5** (rebuild and
register the §Directory Inventory), then 6, 7, 8, 10 in order.

## Phase status

| Phase | Status | Commit |
|-------|--------|--------|
| 1 Record KEEP decision, freeze baseline | COMPLETED | `a5efa8c05` |
| 2 Archive-aware inventory generator | COMPLETED | `4e423448c` |
| 3 Six subtree READMEs, `#exit` guards, root-file policy | COMPLETED | (phase 3 commit) |
| 4 Generated archive counts | COMPLETED | `74116ebb2` |
| 9 Retire duplicate counts + two-archive docstring | IN PROGRESS | -- |
| 5, 6, 7, 8, 10 | NOT STARTED | -- |

## Decisions taken (do not re-litigate)

- **Phase 2 Scope Hypothesis CONFIRMED**: `scripts/lib/live_walk.py` needed no change.
  `live_subdirs("FormalSystem/Boneyard")` returns the 39 subtrees; `live_files` returns all 168.
- **Phase 2 mechanism**: `markdown_targets()` no longer prunes `Boneyard`; `scan()` gained
  `is_archive_base()` + archive-shaped `rows=totals`, and an `empty=skip|include` option so
  README-only tombstones get rows.
- **Phase 3 `#exit` policy**: option (a), MANDATORY. All 168 archived files now carry `#exit`;
  eleven that had no archival marker at all also gained the `ARCHIVED (Boneyard)` banner.
- **Phase 3 loose file**: `VacuousKEquiv.lean` STAYS at the archive root. §Expected File Structure
  amended to admit it; zero live-tree diff. Do not move it.
- **Phase 5 mechanism (decided, not yet applied)**: use a GENERATED block, not hand-maintained
  registration — `rows=both cols=files-lines link=yes empty=include sort=lines-desc`, which
  yields 40 rows (39 subtrees + the loose file) and one hand-written trailing column. The
  six-column table collapses to `| Directory | Files | Lines | Why Archived |`; the `Task` column
  is dropped (Phase 6's D4 work) and the `**Total** | **93** | **58,738**` row is removed in
  favour of §One Archive's generated counts.
- **Phase 6 C9 decision (measured, not yet applied)**: 90 C9-shaped occurrences across 34 files
  under the archive, only 2 in the top-level README, 69 in `.md` / 22 in `.lean`. Tightening C9
  to drop the `/Boneyard/` exclusion would turn the gate RED over 88 occurrences this task does
  not clear. Clear the 2 in the top-level README and RECORD the exclusion with its measurement.

## Provenance SHAs recovered (Phase 6 input, already measured)

Per-archival-event history survives under the pre-rename path `Theories/Bimodal/Boneyard/<name>`;
`--follow` on the current path stops at the tree-wide rename `5359fef7d`. The full 40-row mapping
is in `/tmp/.../scratchpad/provenance.txt` and reproducible with:

```bash
git log --format='%h %ad' --date=short --reverse -- \
  "Theories/Bimodal/Boneyard/<name>" "Boneyard/<name>" "FormalSystem/Boneyard/<name>" | head -1
```

## Concurrency hazard (STANDING)

A separate session is executing task 552 (`WorldHistory` -> `ConvexHistory`) in the same working
tree. Residual gate findings attributable to it, NOT to this task:

- `C11` — `ChainCompleteness/Bundle/SuccChainWorldHistory.lean:3` imports the renamed module
- `C20` tier 1 — two `ReynoldsBridge.lean:NNN` citations in `CompletenessDedekind.lean`
- `INV` / `C5` / `C12` — transient, whenever their live-tree line counts move

Rules being followed: commit only through `git-commit-scoped.sh` with an explicit pathspec; never
run an unguarded `lake build` (one was killed mid-Phase-2 when it collided with their guarded
build); treat a check as green for this task when its only findings name `WorldHistory` or
`ReynoldsBridge`.

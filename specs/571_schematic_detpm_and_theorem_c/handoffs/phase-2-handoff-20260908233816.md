# Phase 2 handoff

- **Next action**: Phase 3 (module docstring + choice bookkeeping) and Phase 4 (prose inventories),
  which are the same wave and touch disjoint files.
- **State**: `detPM_of_deterministic (hD) (φ : StarFormula) : F.StarValidOn (detPM φ)` is schematic
  and consumes `settledDisj_of_deterministic` exactly as `sentDet_of_deterministic` does.
  `deterministic_starDefinable` is the three-way conjunction of biconditionals hinged on
  `F.Deterministic`. `deterministic_of_detPM` is byte-identical to its Phase 1 state (hypothesis
  at atoms), confirmed by `git diff`.
- **Measured axiom sets** (post-change, `lake env lean` on a scratch `import FormalSystem`):
  all five of `settledDisj_of_deterministic`, `sentDet_of_deterministic`,
  `detPM_of_deterministic`, `deterministic_of_detPM`, `deterministic_starDefinable` report
  `[propext, Classical.choice, Quot.sound]` — identical, so the choice asymmetry must be written
  structurally and never as an axiom-set difference.
- **Verification**: `lake build` green (2638 jobs); `lake build BimodalTest` green (2689 jobs).
- **Deviations**: none.

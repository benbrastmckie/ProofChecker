# Phase 1 handoff

- **Next action**: Phase 2 — restate `detPM_of_deterministic` schematically in `φ`, and replace
  `deterministic_starDefinable` with the three-way equivalence pinned in the plan's
  `## Lean Challenge Statements`.
- **State**: `detPM (φ : StarFormula)` and `detPM_unfold (… φ : StarFormula)` are widened.
  `detPM_of_deterministic` and `deterministic_starDefinable` carry holding retypes at
  `detPM (StarFormula.atom p)`. `deterministic_of_detPM`'s hypothesis is at atoms and must stay there.
- **Verification**: `lake build` green (2638 jobs); `lake build BimodalTest` green (2689 jobs);
  zero `sorry` in the module.
- **Decisions**: `detPM (StarFormula.atom p)` is definitionally the pre-change `detPM p`, so
  `deterministic_of_detPM`'s proof body compiled unchanged — this is a retype, not a weakening.
- **Deviations**: none.

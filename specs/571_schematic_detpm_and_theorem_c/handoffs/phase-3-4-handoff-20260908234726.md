# Phase 3 and Phase 4 handoff

- **Next action**: Phase 5 — the full gate (`lake build`, `lake build BimodalTest`,
  `bash scripts/check-module-invariants.sh`, final axiom re-measure, scope-hypothesis closure).
- **State**: `StarDeterminism.lean`'s module docstring now carries the three-way statement in its
  numbered opener, `## Main Definitions` / `## Main Results`, the rewritten
  `## The single sentence letter is not uniform substitution` section (drift-frame counterexample
  intact) and the rewritten `## Choice dependence` section, which states the asymmetry
  structurally and names the measured axiom set explicitly. `detPM`,
  `detPM_of_deterministic`, `deterministic_of_detPM` and `deterministic_starDefinable` docstrings
  are rewritten. `FormalSystem/StarLanguage/README.md`'s Theorem C row and
  `FormalSystem/Semantics/README.md`'s `StarDeterminism.lean` row name the three-way statement.
- **Prose sites confirmed needing no edit**: `FormalSystem/StarLanguage.lean:29`,
  `FormalSystem/Metalogic/Independence/README.md` result 7,
  `FormalSystem/Metalogic/Independence/ForwardDeterministicFrame.lean`'s `fn_separates` docstring,
  and the "Where the L⋆ semantics lives" row (no declaration name moved).
- **Measured axiom sets** (re-measured after the docstring pass): all five report
  `[propext, Classical.choice, Quot.sound]`. Both `[propext, …]` figures asserted in the file
  equal this value.
- **Verification**: `lake build` green (2638 jobs); `lake build BimodalTest` green (2689 jobs);
  `bash scripts/readme-lint.sh FormalSystem/StarLanguage FormalSystem/Semantics` RESULT: PASS with
  no new finding; no task number under `FormalSystem/`.
- **Deviations**: none.

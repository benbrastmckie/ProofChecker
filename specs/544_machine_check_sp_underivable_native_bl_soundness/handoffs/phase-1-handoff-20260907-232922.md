# Phase 1 handoff — Native BL frame and truth

- **Next action**: Phase 2/3 (`FormalSystem/Metalogic/Conservativity/SpCountermodel.lean`) —
  file is already written and elaborates clean under `lake env lean`; close phases, then Phase 4
  (wiring + record correction + full gate).
- **State**: `FormalSystem/Semantics/BLFrame.lean` created (~300 lines), wired into
  `FormalSystem/Semantics.lean` (import + `## Submodules` bullet) and
  `FormalSystem/Semantics/README.md`. Full `lake build` green (2611 jobs, exit 0).
  `bash scripts/readme-lint.sh FormalSystem` PASS.
- **Key decisions**:
  - The characterization family was widened from the plan's enumerated 11 to 13, adding
    `bot_false` and `always_iff`, because the Scope Hypothesis's own check against
    `BLTruth`'s namespace showed those two exist there. Mirror is now genuinely one-for-one.
  - Imports are `FormalSystem.BaseLanguage.Formula` + `Mathlib.Tactic.Push` +
    `Mathlib.Tactic.Tauto`; no Mathlib order import is needed (the frame carries its own order
    fields), so the plan's "Mathlib order imports" reduced to the two tactic imports actually used.
  - `open FormalSystem.Syntax` is required alongside `FormalSystem.BaseLanguage` because
    `autoImplicit false` makes an unqualified `Atom` a hard error.
- **Deviations**: characterization-family widening (annotated inline on the plan checklist).

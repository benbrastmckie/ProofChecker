# Phase 2+3 handoff — native BL soundness, countermodel, headline results

- **Next action**: Phase 4 — wire `SpCountermodel` into `FormalSystem/Metalogic/Conservativity.lean`
  (import, module table row, dependency-chain sentence, both CEB status passages), update
  `SpWitness.lean`'s "What this does not do", add the README row, correct the CEB status rows in
  `README.md` / `docs/theorem-index.md` where they name a citation, then run the full gate.
- **State**: `FormalSystem/Metalogic/Conservativity/SpCountermodel.lean` created (395 lines,
  0 sorry). Scoped `lake build FormalSystem.Metalogic.Conservativity.SpCountermodel` green.
  Axiom profiles confirmed: `not_derivable_sp`, `tmCompleteBase_refuted`,
  `SpCountermodel.blFrameValid_of_derivation`, `SpCountermodel.axiom_valid`, `sp_false` all
  `[propext, Classical.choice, Quot.sound]`; `Semantics.truth_swap` axiom-free.
- **Key decisions**:
  - Name collision: `FormalSystem.Metalogic.axiom_valid` already exists (BL+ version in
    `Metalogic/Soundness.lean`), so the two soundness ingredients live in a nested
    `namespace SpCountermodel`. Signatures unchanged. Deliverables stay at `FormalSystem.Metalogic.*`.
  - Census confirmed by exhaustiveness, not by counting: 16 `Axiom` constructors (13 Base-admissible,
    3 excluded by side condition) and 7 `DerivationTree` constructors.
  - No `#print axioms` in the live module (matches `Z1Countermodel.lean`).
- **Deviations**: the nested namespace (annotated inline on the plan checklist).

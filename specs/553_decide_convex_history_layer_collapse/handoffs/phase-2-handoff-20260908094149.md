# Phase 2 handoff — task 553

- **Next action**: Phase 3 — `probes/02_alternative-consequence.lean` (C2/C3/C4 as Lean defs) and report §3.
- **State**: report §0–§2 written; probe 01 compiles sorry-free; Phase 2 [COMPLETED].
- **Key findings to carry**: the bounded-index reading is a third, unintended hybrid — atom clause
  domain-relative, box clause `H_F`-relative, tense clauses `D`-relative, evaluation time
  unrestricted; `modal_t` is REFUTED at `(bdd, 1)` and HOLDS at `(bdd, 0)`, so the degeneracy is
  exactly `x ∉ dom τ`. 97 live declarations bind an ungated `ConvexHistory`.
- **Probe reusables**: `Probe553A.NF`, `MT`, `totalHist`, `bdd` — copy into probe 02.
- **Deviations**: none.

# Phase 2 handoff — StarFormula syntax landed

- **Next action**: Phase 3.1 (`Semantics/StarTruth.lean`, transcribe probes Parts A, B, E).
- **State**: `FormalSystem/StarLanguage/Formula.lean` + `README.md`; scoped build green; all `rfl` pins pass.
- **Naming**: conjunction/disjunction are `StarFormula.and`/`.or` (Formula's names), not `conj`/`disj`.
  Later phases must use `.and` where the plan writes `conj`. `ofCtx` is `List.map ofFormula Γ`
  (`Γ.map` resolves to `Context.map`, which is Formula-only).
- **Deviations**: naming only (annotated in plan).

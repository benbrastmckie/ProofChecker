# Phase 5 handoff — task 528

**Immediate next action**: open Phase 6 — port `UltrafilterMCS.lean` onto `Order.PrimeFilter LindenbaumAlg`
in the plan's commit order: imports; `toQuot_mem_mcsToSet_iff`; `mcsToPFilter` group; `ultrafilterToSet`
substitution pass; `ultrafilterEquiv`; `ultrafilter_correspondence` corollary; `:910-966` replacement;
deletions last.

**State at handoff**: Phases 1-5 closed. `FormalSystem/ForMathlib/Order/PFilter.lean` (250 lines) is
reachable from the root, sorry-free, imports only Mathlib, and includes the PrimeSeparator corollary.

**Deviations**: Phase 5's optional `.claude/context/repo/project-overview.md` courtesy edit skipped (rule
`source-store-deploy-boundary.md`); annotated inline in the plan.

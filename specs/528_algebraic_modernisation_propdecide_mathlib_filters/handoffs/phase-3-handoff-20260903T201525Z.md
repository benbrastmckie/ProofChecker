# Phase 3 handoff — task 528

**Immediate next action**: open Phase 4 — restate `fold_le_of_derives` (`UltrafilterMCS.lean:565`) over
`((L.map toQuot : List _) : Multiset _).inf`, delete the `fold_from_x` `have`, update the single call
site inside `ultrafilterToSet_mcs` (atomic batch: do not commit the intermediate red state).

**State at handoff**: Phases 1-3 closed. Re-verification evidence already gathered for Phase 4:
`Multiset.inf`, `inf_coe`, `le_inf`, `inf_le`, `inf_zero`, `inf_cons`, `cons_coe` all resolve at the pin
with `[SemilatticeInf α] [OrderTop α]`, and `LindenbaumAlg` has both instances (`inferInstance` checks
pass). `fold_le_of_derives` has exactly one call site (`UltrafilterMCS.lean:718`) and zero references
elsewhere in live `.lean`/`.md`.

**Deviations**: none.

# Phase 4 handoff — task 528

**Immediate next action**: open Phase 5 — create `FormalSystem/ForMathlib/Order/PFilter.lean` (transcribed
from report 02 Appendix A), the C8 sibling `FormalSystem/ForMathlib.lean`, add
`import FormalSystem.ForMathlib` to `FormalSystem/FormalSystem.lean` (confirmed: the repo-root
`FormalSystem.lean` only re-exports it), then doc rows in `FormalSystem/README.md` (aggregator table
`:219-226`, layer table `:235-257`, subdirectory table `:271-274`) and `CLAUDE.md` (`:17-20`).

**State at handoff**: Phases 1-4 closed. `fold_le_of_derives` is over `Multiset.inf`; `fold_from_x` gone;
`UltrafilterMCS.lean` at 1,027 lines. Phase 6 evidence already gathered: every live `Ultrafilter` hit
outside `UltrafilterMCS.lean` is Mathlib's (`Bundle/LimitMCS`, `Semantics/Ultraproduct/*`, `Compactness`,
prose in `Semantics.lean`/`ChronicleRealExtension.lean`); the only external mention of
`ultrafilter_correspondence` is `Algebraic/README.md:151` (Phase 7).

**Deviations**: none.

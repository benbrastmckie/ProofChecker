# Code Review Report

**Date**: 2026-09-07
**Scope**: all (repository health check — active `.lean` tree, task/roadmap state, in-flight work)
**Reviewed by**: Claude

## Summary

- Total files reviewed: targeted audit (sorry/TODO/FIXME sweep across 623 `.lean` files under
  `FormalSystem/`, plus follow-up reads on the two files the sweep surfaced); no full re-read of the
  ~115-file territory sweep already completed six days ago in `review-2026-09-01-lean-engineering.md`
- Critical issues: 0
- High priority issues: 0
- Medium priority issues: 1
- Low priority issues: 0

**Context**: this repo already runs a tight review/task loop (21 prior reviews, 53 tasks created,
last review 2026-09-01 with 122 findings across 6 critical / 31 high / 62 medium / 23 low). 12 of
that review's 14 spawned tasks (518–529, including both Criticals — the global `simp` loop and the
four modules missing from the build graph) are already completed and archived; only the two Wave 5
publication-infrastructure tasks (530, 531) remain open. A second from-scratch multi-territory sweep
six days later would mostly re-derive that report, so this pass instead verified overall health and
looked for anything new since then.

**Concurrent work in progress**: task 546 (rename `FrameClass.Discrete`/`.Dedekind` to
`.ZTime`/`.RTime`) is mid-flight in another session (plan phase 7/9, `[IMPLEMENTING]`), with its own
`lake build` running throughout this review. This review did not touch any file in task 546's scope
and stopped a duplicate `lake build` it had started, rather than contend for CPU with the in-progress
build-guard. Build-green verification for the current tree is therefore deferred to that task's own
gate rather than independently re-run here.

## Medium Priority Issues

### M1. Stale "sorry'd" docstring on a theorem that is now fully proved [FIXED]
**File**: `FormalSystem/Metalogic/WeakCanonical/Expressiveness/SplitPoint.lean:155-157`
**Description**: The docstring on `obtain_split_point_props` (theorem body at `:159-950`, from the
archived Reynolds-pipeline task) reads:

> NOTE: Steps 2-5 are sorry'd pending full proofs of strategy_restrict_left/right and the
> sub-interval h_pt witness (existence of an actual point in each sub-interval). The construction
> is structurally correct — the sorry's are in the strategy restriction lemma and the sub-interval
> point existence.

The theorem body contains no `sorry` (confirmed by exact-match search across the whole file), calls
the real `ghr93_strategy_restrict_left`/`_right` theorems from `EFGames/CustomGame.lean:1254,1488`
(themselves sorry-free, outside `Boneyard/`), and a second stale comment at `:192,194` similarly
says "we sorry-construct the infimum ... and is sorry-free" about code that no longer contains a
`sorry`. This module is imported into the live tree (`Expressiveness/CaseAnalysis.lean`), so the
comment is read as current status by anyone landing on the declaration.
**Impact**: Low correctness risk (the proof is real), but actively misleading: a reader — or a future
sorry-inventory/C14-style audit relying on docstring text rather than a build-time count — would
believe this flagship completeness lemma still has open sub-obligations when it does not.
**Fix applied** (this session, comment-only, no proof content changed): rewrote both stale
paragraphs (`:155-157` and `:192,194`) to state the actual sorry-free status and name the real
theorems (`ghr93_strategy_restrict_left`/`_right`) that discharge the steps. Not independently
re-verified with `lake build` in this session (a concurrent build for task 546 was already in
flight; the edit touches only comments inside an existing doc-comment block, outside task 546's
file scope).

## Code Quality Metrics

| Metric | Value | Status |
|--------|-------|--------|
| Active-tree `sorry` (exact-match, outside `Boneyard/`) | 0 | OK |
| `Boneyard/`-confined `sorry` (archived, unimported dead code) | 102 | Info |
| TODO/FIXME/QUESTION tags, active tree | 0 | OK |
| NOTE tags, active tree | 58 (7 files; 6 benign historical notes, 1 stale — M1) | OK |
| `nolints.json` grandfathered baseline entries | 307 | Info |
| Build status | Not independently re-verified (concurrent build-guard in flight) | Deferred |

## Roadmap Progress

### Completed Since Last Review
`roadmap-integration.sh --annotate` found 22 candidate task→checkbox matches, all `low_confidence`;
0 were auto-applied. No high-confidence completions to annotate this run — the existing roadmap
checkboxes appear current.

### Current Focus
| Phase | Priority | Current Goal | Progress |
|-------|----------|--------------|----------|
| 1 | Medium | Weak and Strong Completeness (strong-completeness capstone open) | 5/7 |
| 2 | Medium | Decidability and the Tableau Engine (largest open front) | 1/7 |
| 3 | — | Kamp Theorem and Expressive Completeness | 3/3 (done) |
| 4 | — | Finite Model Property and Decidable Model Checking | 1/4 |
| 5 | — | Publication and Documentation | 1/4 |
| 6 | Medium | Dataset and Training Infrastructure (carried forward) | 0/10 |
| 7 | — | Repository Hygiene and Programme Metadata | 3/4 |

### Recommended Next Tasks
1. Fix M1 above (trivial, no dependencies) — good small-scope filler task.
2. Resume tasks 530/531 (Wave 5 publication infrastructure) — the last two open items from the
   2026-09-01 review; task 530 is `[RESEARCHED]` and ready to plan.
3. Let task 546 finish its remaining phases (7-9: docstring rewrite, Typst prose, final
   verification gate) before starting new work in its file scope
   (`FormalSystem/`, `scripts/check-module-invariants.sh`, `scripts/nolints.json`, etc.).

## Recommendations

1. M1 was fixed directly in this session (comment-only edit, no task opened — the user confirmed
   task 546's Phase 7 had already finished and Phase 8 hadn't started, and the file is outside
   task 546's scope, so a same-day direct fix was preferable to a competing task).
2. No other action items from this pass; the existing review/task cadence (21 reviews, 53 tasks,
   96% of the last review's items already resolved in 6 days) is functioning well and does not need
   a competing large task queue on top of it right now.

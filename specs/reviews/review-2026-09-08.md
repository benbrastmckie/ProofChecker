# Code Review Report

**Date**: 2026-09-08
**Scope**: all (roadmap-focused, per author directive)
**Reviewed by**: Claude

## Summary

This review was scoped by the author's explicit directive: *"I'm looking to finish the
foundations and all small results, avoiding open mathematics and long term efforts like
decidability for now. Help me identify what I should work on next, updating the ROADMAP.md
accordingly."* Rather than a file-by-file TODO/FIXME/code-smell scan, this review (1) ran the
roadmap-integration pass to check `specs/ROADMAP.md` against current task state, (2) classified
every active task against the author's foundations/small-results-only constraint, and (3) updated
`specs/ROADMAP.md` with a near-term work plan reflecting that constraint.

- Active tasks reviewed: 48 (post-archival; 23 tasks were archived by `/todo` immediately before
  this review)
- Stale roadmap claims found and fixed: 1 (task 362, see below)
- Tasks classified as in-scope (foundations/small results): 14, ordered in the new near-term plan
- Tasks classified as deferred (open mathematics / long-horizon): 27, across 8 fronts
- Critical/High/Medium/Low code issues: not applicable — no TODO/FIXME/code-smell scan was run
  this pass (see Scope note above)

## Stale Roadmap Claim Found

### Task 362 marked "not_started" while already completed
**File**: `specs/ROADMAP.md:71` (pre-edit)
**Description**: The Phase 1 bullet read `**Consequence-completeness capstone (task 362,
not_started)**` — self-contradictory, since the same bullet was already checked `[x]` and tagged
`*(Completed: Task 362)*`. `specs/state.json`/`specs/archive/state.json` confirm task 362's status
is `completed`.
**Impact**: The `## Recommended Priority Order` section at the end of the document listed this
capstone as item #2, described as merely "unblocked", which understates its actual status.
**Fix applied**: Corrected the Phase 1 bullet text and the Recommended Priority Order entry to
state `DONE` plainly. (The automated `roadmap-integration.sh --annotate` pass found 22
candidate matches against completed tasks, all below the auto-annotate confidence threshold —
none applied automatically; this one was caught and fixed by hand during manual cross-reference.)

## Roadmap Progress

### Roadmap Signal
- **Structure**: 7 phase(s), 39 checkbox(es), 60 table row(s) — parseable: true
- **Warnings**: none
- **Skipped**: 22 item(s) skipped by the automated annotation pass — reasons: low_confidence (all
  22; the roadmap's prose-heavy item text doesn't string-match task titles closely enough for the
  script's heuristic — expected given this document's citation-heavy writing style)

### Near-Term Work Plan (written into ROADMAP.md this pass)

A new `## Near-Term Work Plan: Foundations and Small Results` section was inserted into
`specs/ROADMAP.md` (after the Overview, before Phase 1), and the existing `## Recommended
Priority Order` section (which puts the decidability spine first) was annotated as superseded
for near-term scheduling by the new section, while being left intact as the durable full-programme
record.

**Recommended next tasks, in order** (foundations/small-results only):

1. **562** `sync_language_names_with_paper_l_minus_plus_star` — pure rename/prose sweep
2. **540** `docstring_coverage_class_instance_lemma` — close docstring-coverage floor gaps
3. **542** `dead_declaration_triage_c17_findings` — dead-declaration census triage
4. **506** `fix_typst_display_defects_via_playwright_visual_loop` — typst display fixes
5. **178** `publication_examples_and_demo` — propositional-fragment demo (decidable today)
6. **193** `codebase_tactic_refactor` — soundness-layer tactic macros
7. **568** `c3_c4_consequence_relations_as_library_definitions` — promote probe results to library
8. **569** `retarget_semantics_to_possible_world_index` — promote already machine-checked argument
9. **565** `totality_and_directed_gluing_from_extension_theorem` — wrappers on a proved theorem
10. **566** `possible_worlds_clause_hf_as_limit` — uses an existing proved hook
11. **564** `sheaf_clause_gluing_and_starpasting_generalization` — composition step already proved
12. **563** `formalize_interval_site_and_behavior_presheaf` — presheaf skeleton promotion
13. **567** `determinism_clause_and_separatedness_asymmetry` — connects to a proved lemma
14. **543** `formalize_mf_correspondence_rigidity` — machine-checks externally-researched results
    (largest item in this list; re-confirm scope before starting)

Independent, non-mathematical, small-and-bounded track (optional, parallel): the Phase 6
dataset/training-infrastructure cluster (298, 296, 282, 257, 231, 219).

**Deferred fronts** (open mathematics or long-horizon — excluded per the author's directive):

| Front | Tasks | Why deferred |
|---|---|---|
| Decidability/tableau spine (Phase 2) | 410, 411, 412, 428, 429, 430, 464, 465, 481, 482 | Largest open front; several tasks explicitly self-labeled open mathematics |
| Semantic finite model property (Phase 4) | 476 | Explicitly "OPEN MATHEMATICS. MULTI-MONTH." |
| TM⋆ completeness / non-definability | 537, 559, 560, 561 | Multi-phase completeness programme for the extended language |
| H/G-fragment finite axiomatizability | 534 | Open research question |
| C3 completeness question | 570 | Explicitly "OPEN RESEARCH QUESTION, not an implementation task" |
| Jønsson-Tarski algebraic representation | 125, 497, 498, 499, 500, 501, 502, 504 | Capstone plus five-phase groundwork; underlying ultraproduct work not yet scoped |
| Object-language extensions | 127, 128 | Roadmap's own Phase 6 note recommends ABANDON or park |
| Documentation final-polish | 177 | Explicitly gated on the (deferred) decidability chain landing |

### Roadmap Signal reminder
No `unparseable_roadmap` or `annotation_noop` warnings this run — the roadmap parses cleanly and
the annotation pass genuinely found nothing above its confidence bar, not a masked failure.

## Recommendations

1. Work the near-term list above roughly in order; items 7-13 form a tight cluster (probe
   promotion + presheaf/sheaf dictionary) that likely benefits from being run as one connected
   pass rather than 7 separate dispatches, since 563 is infrastructure the others build on.
2. Task 543 is the one item on this list whose scope is not yet tightly bounded (six external
   reports to read first) — verify its actual size before committing to it early in the sequence;
   it may be worth moving later or splitting.
3. Revisit the deferred-fronts table once the author lifts the open-mathematics/decidability
   restriction — `## Recommended Priority Order` at the end of `specs/ROADMAP.md` already has the
   full-programme resumption plan ready to go.

## Scope Note

This review did not run a source-level TODO/FIXME/code-smell scan or the standard multi-task
issue-grouping-and-creation flow (`.claude/scripts/issue-grouping.sh` / `tier-selection.sh`) —
the author's request was specifically a roadmap-prioritization judgment call over the *existing*
task backlog, not new-issue discovery. No new tasks were created this pass; the 14
near-term-track tasks and the 27 deferred tasks already exist in `specs/state.json`.

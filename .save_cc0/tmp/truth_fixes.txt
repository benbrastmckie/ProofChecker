TRUTH.LEAN COMPILATION FIXES - Iteration 3

## Issue Summary
After generalizing from Int to LinearOrderedAddCommGroup T, the file has 30+ errors in 3 categories:

### Category 1: Group Lemma Pattern Mismatches (Lines 350, 361, 407, 433, 469, 495)
- omega tactic no longer works for polymorphic T
- Need to use Mathlib lemmas: add_sub_cancel, sub_add_cancel, add_comm, etc.

### Category 2: Variable Naming Conflicts (Lines 557-1287)
- F is both a type parameter (line 83) and local variables in `is_valid` definition
- Need to rename F parameter in is_valid to avoid collision
- NOT a simple find/replace - must preserve F in TaskFrame F, TaskModel F types

### Category 3: Missing Typeclass Instances (scattered)
- Need proper Frame parameter threading

## Fix Strategy

1. Replace all `by omega` with proper group lemma proofs
2. Fix is_valid definition to use different parameter name
3. Update all theorems that use is_valid

## Key Lemmas
- add_sub_cancel a b : a + b - b = a
- sub_add_cancel a b : a - b + b = a
- add_sub_cancel' a b : a + (b - a) = b
- add_comm : a + b = b + a
- sub_sub_self a b : a - (a - b) = b
- add_sub_cancel_left a b : a + b - a = b
